# === CONFIGURACIÓN PRINCIPAL ===
# Bancos a procesar (módulos en banks/)
BANKS=brou_personas,oca
# Clave Fernet para cifrado (generar con: python setup.py --generate-key)
CREDS_KEY=

# === SCHEDULER ===
# Horarios de ejecución (HH:MM separados por coma)
SCHEDULE_HOURS=07:00,20:00
# Minutos aleatorios agregados al horario para evitar patrones
RANDOM_DELAY_MIN=30
# Zona horaria
TZ=America/Montevideo

# === MQTT (Home Assistant) ===
MQTT_ENABLED=true
MQTT_BROKER=192.168.1.50
MQTT_PORT=1883
MQTT_USER=
MQTT_PASS=
# Prefijo de tópicos
MQTT_TOPIC_PREFIX=banks

# === SERVIDOR HTTP (alternativo) ===
HTTP_PORT=8000
# IP/Host para construir URLs de logos en el JSON
HTTP_HOST=192.168.1.100
# Lista de IPs permitidas (vacío = permite todas)
ALLOWED_IPS=

# === AVANZADO ===
# Directorio para credenciales cifradas
CREDENTIALS_DIRECTORY=/dev/shm/creds
# Logs de geckodriver (1=activar, 0=desactivar)
GECKODRIVER_LOGS=0
# Modo headless (1=sin GUI)
HEADLESS=1
